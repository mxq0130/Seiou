<section class="hero-root">
  <!-- 背景图 + 深色遮罩 -->
  <div class="hero-bg">
    <img
      src="https://images.unsplash.com/photo-1558618666-fcd25c85f82e?w=1920&h=1080&fit=crop"
      alt=""
      class="hero-img"
      fetchpriority="high"
    />
    <div class="hero-overlay"></div>
  </div>

  <!-- 几何光效 -->
  <div class="hero-lights" aria-hidden="true">
    <div class="light light-1"></div>
    <div class="light light-2"></div>
    <div class="light light-3"></div>
  </div>

  <!-- 内容 -->
  <div class="hero-content">
    <p class="hero-eyebrow">PERSONAL BLOG</p>
    <h1 class="hero-title">
      <span class="title-line">创造与分享</span>
      <span class="title-line accent">皆为热爱</span>
    </h1>
    <p class="hero-desc">
      一个二次元爱好者的技术博客。记录前端开发、动画追番与生活随笔。
    </p>
    <div class="hero-actions">
      <a href="/posts" class="btn-primary">
        <span>浏览文章</span>
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 17l9-5-9-5"/></svg>
      </a>
      <a href="/about" class="btn-ghost">关于我</a>
    </div>
  </div>

  <!-- 底部滚动线 -->
  <div class="scroll-hint" aria-hidden="true">
    <div class="scroll-line"></div>
  </div>
</section>

<style>
  .hero-root {
    position: relative;
    width: 100vw;
    height: 100vh;
    height: 100dvh;
    overflow: hidden;
    margin-left: calc(-50vw + 50%);
    margin-right: calc(-50vw + 50%);
  }

  /* 背景 */
  .hero-bg {
    position: absolute;
    inset: 0;
  }
  .hero-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transform: scale(1.05);
    animation: heroDrift 20s ease-in-out infinite alternate;
  }
  @keyframes heroDrift {
    0% { transform: scale(1.05) translate(0, 0); }
    100% { transform: scale(1.08) translate(-1%, -0.5%); }
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      135deg,
      rgba(30, 20, 40, 0.75) 0%,
      rgba(30, 20, 40, 0.45) 50%,
      rgba(20, 15, 35, 0.7) 100%
    );
  }

  /* 几何光效 */
  .hero-lights { position: absolute; inset: 0; overflow: hidden; }
  .light {
    position: absolute;
    border-radius: 50%;
    filter: blur(100px);
    opacity: 0.12;
    animation: lightPulse 8s ease-in-out infinite alternate;
  }
  .light-1 {
    width: 600px; height: 600px;
    top: -200px; right: -100px;
    background: #E8738A;
    animation-delay: 0s;
  }
  .light-2 {
    width: 450px; height: 450px;
    bottom: -100px; left: -50px;
    background: #6B7DB3;
    animation-delay: 3s;
  }
  .light-3 {
    width: 300px; height: 300px;
    top: 40%; left: 50%;
    background: #7EC8E3;
    animation-delay: 6s;
  }
  @keyframes lightPulse {
    0% { opacity: 0.06; transform: scale(1); }
    100% { opacity: 0.16; transform: scale(1.15); }
  }

  /* 内容 */
  .hero-content {
    position: absolute;
    bottom: 18%;
    left: 0;
    right: 0;
    padding: 0 clamp(1.5rem, 8vw, 8rem);
    z-index: 2;
  }

  .hero-eyebrow {
    font-size: 0.75rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: rgba(255,255,255,0.5);
    margin-bottom: 1.5rem;
    font-weight: 500;
    font-family: var(--font-mono);
  }

  .hero-title {
    display: flex;
    flex-direction: column;
    gap: 0.15em;
    margin-bottom: 1.5rem;
  }

  .title-line {
    font-size: clamp(3rem, 8vw, 6.5rem);
    font-weight: 800;
    line-height: 1.05;
    color: #fff;
    letter-spacing: -0.02em;
  }
  .title-line.accent {
    background: linear-gradient(135deg, #F0A0B5 0%, #9DAFDE 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-desc {
    font-size: clamp(0.95rem, 1.8vw, 1.2rem);
    color: rgba(255,255,255,0.55);
    max-width: 520px;
    line-height: 1.7;
    margin-bottom: 2.5rem;
  }

  /* 按钮 */
  .hero-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.85rem 2rem;
    border-radius: 9999px;
    background: rgba(255,255,255,0.95);
    color: #1a1a2e;
    font-weight: 600;
    font-size: 0.95rem;
    text-decoration: none;
    transition: all 0.3s ease;
    backdrop-filter: blur(10px);
  }
  .btn-primary:hover {
    background: #fff;
    transform: translateY(-2px);
    box-shadow: 0 12px 40px rgba(0,0,0,0.3);
  }

  .btn-ghost {
    display: inline-flex;
    align-items: center;
    padding: 0.85rem 2rem;
    border-radius: 9999px;
    border: 1.5px solid rgba(255,255,255,0.3);
    color: rgba(255,255,255,0.85);
    font-weight: 500;
    font-size: 0.95rem;
    text-decoration: none;
    transition: all 0.3s ease;
    backdrop-filter: blur(10px);
  }
  .btn-ghost:hover {
    border-color: rgba(255,255,255,0.6);
    background: rgba(255,255,255,0.08);
    transform: translateY(-2px);
  }

  /* 滚动提示 */
  .scroll-hint {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    z-index: 2;
  }
  .scroll-line {
    width: 1px;
    height: 40px;
    background: linear-gradient(to bottom, rgba(255,255,255,0.4), transparent);
    animation: scrollLine 2s ease-in-out infinite;
  }
  @keyframes scrollLine {
    0%, 100% { transform: scaleY(0.4); opacity: 0.3; }
    50% { transform: scaleY(1); opacity: 1; }
  }
</style>
