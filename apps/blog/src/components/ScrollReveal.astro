---
export interface Props {
  type?: 'fade-up' | 'fade-left' | 'fade-right' | 'scale';
  delay?: number;
}

const { type = 'fade-up', delay = 0 } = Astro.props;
---

<div class="sr" data-sr-type={type} data-sr-delay={delay}>
  <slot />
</div>
