# UI/UX 设计规范

## 设计风格

二次元动漫风格博客。设计参考 Mizuki 的布局和功能结构，但视觉上做差异化——不走 Material Design 3 路线，而是采用更柔和、更有温度的「樱花与星夜」动漫主题。

### 设计关键词
- 🎀 **柔和**：大面积暖色渐变、毛玻璃效果
- 🌸 **治愈**：樱花粉 + 蓝紫渐变的配色
- ✨ **灵动**：微妙的悬浮动画、粒子装饰
- 🌙 **星夜**：深色模式下呈现星空氛围

### 参考来源
- 布局结构参考 [Mizuki](https://github.com/LyraVoid/Mizuki)（功能对齐，风格不同）
- 配色灵感来自日本动画「秒速五厘米」「你的名字」

---

## 配色方案

### 浅色主题 (Light) — 樱花白

| 用途 | 色值 | 说明 |
|------|------|------|
| 主色 (Primary) | `#E8738A` | 樱花粉，按钮、链接、强调元素 |
| 主色浅 | `#FFF0F3` | 主色浅背景 |
| 辅色 (Secondary) | `#6B7DB3` | 蓝紫，次要强调、代码块边框 |
| 渐变主色 | `#E8738A → #6B7DB3` | 标题渐变、Banner 遮罩 |
| 背景色 | `#FFFAFB` | 页面主背景（暖白） |
| 表面色 | `rgba(255,255,255,0.8)` | 卡片/侧边栏（毛玻璃） |
| 文字主色 | `#2D2B3D` | 正文文字（深紫灰） |
| 文字辅色 | `#7B7894` | 次要文字 |
| 边框色 | `#EDE8F0` | 分割线、卡片边框 |
| 樱花粉点缀 | `#FFB7C5` | Tag 背景、图标 |
| 天空蓝点缀 | `#7EC8E3` | 链接、进度条 |

### 深色主题 (Dark) — 星夜紫

| 用途 | 色值 | 说明 |
|------|------|------|
| 主色 (Primary) | `#F0A0B5` | 浅樱花粉（深色下提亮） |
| 主色深 | `#4A2C3D` | 主色深容器 |
| 辅色 (Secondary) | `#9DAFDE` | 浅蓝紫 |
| 渐变主色 | `#F0A0B5 → #9DAFDE` | 标题、Banner |
| 背景色 | `#1A1725` | 页面主背景（深紫黑） |
| 表面色 | `rgba(30,27,46,0.85)` | 卡片/侧边栏（毛玻璃） |
| 文字主色 | `#E8E3F0` | 正文文字 |
| 文字辅色 | `#9B95B5` | 次要文字 |
| 边框色 | `#2E2A40` | 分割线 |

### 状态色

| 用途 | 色值 |
|------|------|
| 成功 | `#6ECCB5` (薄荷绿) |
| 警告 | `#F5C26B` (暖金) |
| 错误 | `#F08080` (浅珊瑚) |
| 信息 | `#7EC8E3` (天空蓝) |

---

## 排版

### 字体

| 用途 | 字体 | 备选 |
|------|------|------|
| 站点标题 | `ZCOOL KuaiLe` (站酷快乐体) | 系统默认中文字体 |
| 文章标题 | `Noto Serif SC` (思源宋体) | 系统宋体 |
| 正文 | `Noto Sans SC` (思源黑体) | 系统默认中文字体 |
| 代码 | `JetBrains Mono` | `Fira Code` |

### 字号

| 级别 | 大小 | 行高 | 用途 |
|------|------|------|------|
| h1 | 2rem / 32px | 1.3 | 页面标题 |
| h2 | 1.5rem / 24px | 1.35 | 区块标题 |
| h3 | 1.25rem / 20px | 1.4 | 子标题 |
| body | 1rem / 16px | 1.7 | 正文 |
| small | 0.875rem / 14px | 1.5 | 辅助文字、元信息 |
| caption | 0.75rem / 12px | 1.4 | 标签、脚注 |

### 文章排版 (Prose)

- 正文行高 1.7，段落间距 1.2em
- 标题与正文间距较大（h2 前 2em，h3 前 1.5em）
- 引用块左侧 3px 渐变边框
- 链接加下划线（hover 渐变色）

---

## 间距系统

基于 4px 基准：

| Token | 值 | 用途 |
|-------|-----|------|
| xs | 4px | 紧凑间距 |
| sm | 8px | 小间距 |
| md | 16px | 标准间距 |
| lg | 24px | 区块间距 |
| xl | 32px | 大区块间距 |
| 2xl | 48px | 页面级间距 |
| 3xl | 64px | Banner/大区块 |

---

## 响应式断点

| 断点 | 宽度 | 布局 |
|------|------|------|
| sm | < 640px | 单列，侧边栏隐藏 → 底部抽屉 |
| md | 640px - 1024px | 双列变体，侧边栏可折叠 |
| lg | > 1024px | 双列：内容+侧边栏，TOC 右侧固定 |
| xl | > 1280px | 双列 + TOC 三列 |

---

## 圆角

| Token | 值 | 用途 |
|-------|-----|------|
| xs | 4px | 小标签 |
| sm | 8px | 按钮、输入框 |
| md | 12px | 卡片、Widget |
| lg | 16px | 大卡片、弹窗 |
| xl | 24px | Banner 内元素 |
| full | 9999px | 头像、药丸形 Tag |

---

## 阴影 & 毛玻璃

| 级别 | 值 | 用途 |
|------|-----|------|
| card | `0 2px 12px rgba(45,43,61,0.06)` | 卡片默认 |
| card-hover | `0 8px 30px rgba(45,43,61,0.10)` | 卡片悬浮 |
| sticky | `0 4px 20px rgba(45,43,61,0.08)` | 吸顶导航 |
| glass | `backdrop-filter: blur(16px) saturate(180%)` | 毛玻璃效果 |

---

## 动效规范

| 类型 | 时长 | 缓动 | 用途 |
|------|------|------|------|
| 微交互 | 150ms | `ease-out` | 按钮/图标 hover |
| 卡片悬浮 | 300ms | `ease-out` | 上浮 + 阴影加深 |
| 主题切换 | 400ms | `ease-in-out` | 全局颜色过渡 |
| 页面入场 | 400ms | `ease-out` | Swup 页面切换 |
| 骨架屏 | 1.5s | `ease-in-out` | 加载闪烁动画 |

---

## 二次元装饰元素

- **樱花粒子飘落**：24 片 CSS 动画花瓣（Hero 区，全屏可见）
- **卡片毛玻璃**：侧边栏、导航栏、Banner 遮罩使用 `backdrop-filter: blur()`
- **渐变标题**：h1 标题使用 `#E8738A → #6B7DB3` 渐变文字
- **卡片底部渐变条**：PostCard / Widget 底部 3px 渐变色条
- **封面图悬浮缩放**：hover 时 `scale(1.03)` + 亮度提升
- **深色模式星空背景**：CSS 生成的随机小点模拟星空

---

## 首页设计

首页采用全屏多区块滚动式布局，代替传统文章列表首页。

### 区块结构

| 区块 | 高度 | 说明 |
|------|------|------|
| Hero 全屏 | 100vh | 渐变背景动画 + 24 片樱花粒子 + 标题呼吸发光 + 打字机副标题 + CTA 按钮 + 向下滚动指示器 |
| 个人卡片 | auto | 毛玻璃卡片悬浮 Hero 下方，含头像/昵称/简介/社交链接 |
| 瀑布流文章 | auto | CSS columns 瀑布流（3→2→1列响应式），分类筛选标签，hover 发光边框 |
| 站点统计 | auto | 4 栏数字滚动动画（IntersectionObserver 触发 easeOutCubic） |
| 快捷入口 | auto | 4 张导航卡片（追番/图集/日记/关于），hover 上浮 |

### 动画清单

| 动画 | 类型 | 触发条件 |
|------|------|----------|
| 渐变背景移动 | CSS `background-position` | 页面加载即开始 |
| 樱花粒子飘落 | CSS `@keyframes fall` | 页面加载即开始 |
| 标题呼吸发光 | CSS `drop-shadow` | 页面加载即开始 |
| 打字机效果 | JS 逐字输出+回删 | 页面加载后 500ms |
| 滚动指示器弹跳 | CSS `translateY` | 持续循环 |
| 区块渐入 | IntersectionObserver + `fadeInUp` | 滚动到 15% 可见 |
| 统计数字滚动 | JS `easeOutCubic` 缓动 | 滚动到 50% 可见 |
| 卡片悬浮上浮 | CSS `translateY` hover | 鼠标悬停 |
| 封面图缩放 | CSS `scale` hover | 鼠标悬停 |
| 发光边框 | CSS `box-shadow` hover | 鼠标悬停 |

### 后台可配置项

通过后台「站点设置」页面可控制：

- 站点标题/副标题/描述/关键词
- 站长昵称/简介/社交链接
- Banner 图片 URL 列表
- 首页公告文字
- 首页各区块开关（Hero/个人卡片/文章/统计/快捷入口）
- 侧边栏 Widget 开关（统计/公告/分类/标签/音乐/日历）
- 页脚文字
